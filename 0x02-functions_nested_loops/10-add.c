#include "main.h"

/**
 * add - adds two integers ands returns the result
 * @a: int to be added to b
 * @b: into to be added to a
 * Return: sum of a and b
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
