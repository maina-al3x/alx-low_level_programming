#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first integer to multiply
 * @b: the second integer to multiply
 * Return: Always 0
 */
int mul(int a, int b);
{
	int mul;

	mul = a * b;
	return (mul);
}
